---
title: zkSend SDK
description: Send Haneul assets through shareable claim links using the zkSend SDK.
keywords: [zkSend, claim links, asset transfer, Haneul, TypeScript, '@haneullabs/zksend', shareable links]
---

The zkSend SDK provides tools for interacting with the zkSend primitive. It provides functionality
to create your own zkSend Claim Links, with support for any publicly transferrable asset.

## Installation

```npm
npm i @haneullabs/zksend @haneullabs/haneul
```

## Setup

To use the zkSend SDK, create a Haneul client and extend it with the zkSend extension:

```ts
import { HaneulGrpcClient } from '@haneullabs/haneul/grpc';
import { zksend } from '@haneullabs/zksend';

const client = new HaneulGrpcClient({
	network: 'mainnet',
	baseUrl: 'http://158.69.54.239:9000',
}).$extend(zksend());
```

The zkSend SDK supports Mainnet and Testnet networks. The extension automatically configures the
correct contract IDs based on the client's network.
