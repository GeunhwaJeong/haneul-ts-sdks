---
title: HaneulGraphQLClient
description: Connect to Haneul through GraphQL with HaneulGraphQLClient.
keywords:
  [
    HaneulGraphQLClient,
    GraphQL,
    Haneul client,
    queries,
    TypeScript SDK,
    Core API,
    '@haneullabs/haneul',
    type-safe,
  ]
---

The `HaneulGraphQLClient` enables type-safe GraphQL queries against the Haneul GraphQL API.

For more details on the Haneul GraphQL API, see the
[GraphQL reference](https://docs.haneul.io/references/haneul-graphql).

The `HaneulGraphQLClient` implements all the the [Core API](/haneul/clients/core) methods:

```typescript
import { HaneulGraphQLClient } from '@haneullabs/haneul/graphql';

const client = new HaneulGraphQLClient({
	url: 'https://haneul-mainnet.haneul-labs.com/graphql',
	network: 'mainnet',
});

const { object } = await client.getObject({ objectId: '0x...' });
```

## Custom GraphQL queries

To query anything no in the Core API, you can use the `query` method to execute custom GraphQL
queries.

We'll start by creating our client, and executing a very basic query:

```typescript
import { HaneulGraphQLClient } from '@haneullabs/haneul/graphql';
import { graphql } from '@haneullabs/haneul/graphql/schema';

const gqlClient = new HaneulGraphQLClient({
	url: 'https://graphql.testnet.haneul.io/graphql',
	network: 'testnet',
});

const chainIdentifierQuery = graphql(`
	query {
		chainIdentifier
	}
`);

async function getChainIdentifier() {
	const result = await gqlClient.query({
		query: chainIdentifierQuery,
	});

	return result.data?.chainIdentifier;
}
```

## Type-safety for GraphQL queries

You might have noticed the example above does not include any type definitions for the query. The
`graphql` function used in the example is powered by [`gql.tada`](https://gql-tada.0no.co/) and will
automatically provide the required type information to ensure that your queries are properly typed
when executed through `HaneulGraphQLClient`.

The `graphql` function detects variables used by your query, and will ensure that the variables
passed to your query are properly typed.

```typescript
const getHaneulnsName = graphql(`
	query getHaneulName($address: HaneulAddress!) {
		address(address: $address) {
			defaultHaneulnsName
		}
	}
`);

async function getDefaultHaneulnsName(address: string) {
	const result = await gqlClient.query({
		query: getHaneulnsName,
		variables: {
			address,
		},
	});

	return result.data?.address?.defaultHaneulnsName;
}
```

## Using typed GraphQL queries with other GraphQL clients

The `graphql` function returns document nodes that implement the
[`TypedDocumentNode`](https://github.com/dotansimha/graphql-typed-document-node) standard, and will
work with the majority of popular GraphQL clients to provide queries that are automatically typed.

```typescript
import { useQuery } from '@apollo/client';

const chainIdentifierQuery = graphql(`
	query {
		chainIdentifier
	}
`);

function ChainIdentifier() {
	const { loading, error, data } = useQuery(getPokemonsQuery);

	return <div>{data?.chainIdentifier}</div>;
}
```
